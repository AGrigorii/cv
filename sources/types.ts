export enum Languages {
    en = 'en',
    ru = 'ru'
}

export enum Pages {
    main = 'main',
    career = 'career',
    cv = 'cv',
    skills = 'skills',
    contacts = 'contacts'
}

export type Language = Languages;
export type Page = Pages;
